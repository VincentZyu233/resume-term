# Resume-Term

Cross-platform workspace manager for terminal layouts and SSH sessions.

## Scope
- Windows x64
- Linux x64

## CI trigger
- Commit message contains `build action` for build
- Commit message contains `build release` for build + release

## Layout
- `lib/` Flutter UI
- `rust/` Rust core
- `docs/` planning and examples

